FROM vulhub/tomcat:9.0.30

RUN apt update

RUN apt install -y nano

COPY tomcat-users.xml /usr/local/tomcat/conf/tomcat-users.xml

COPY tomcat_context.xml /usr/local/tomcat/webapps/manager/META-INF/context.xml

COPY tomcat_to_stdout.sh /tomcat_to_stdout.sh

EXPOSE 8080
CMD ["catalina.sh", "run"]
