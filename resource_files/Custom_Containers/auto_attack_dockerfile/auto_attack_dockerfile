FROM dh157/goibhniu:auto_attack

RUN rm /usr/bin/enumeration

COPY enumeration.py /usr/bin/enumeration

RUN chmod +x /usr/bin/entrypoint

RUN chmod +x /usr/bin/enumeration

CMD ["enumeration"]

ENTRYPOINT ["enumeration"]
